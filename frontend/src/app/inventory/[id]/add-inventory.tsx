"use client";

import { Button } from "@/components/catalyst-ui";

export function AddInventory() {
  return (
    <Button
      type="button"
      onClick={() => alert("Coming soon! The API endpoint for creating inventory is not available yet.")}
    >
      Add Inventory
    </Button>
  );
}
